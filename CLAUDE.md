# village-run

Three.js 3D UI 기반 러닝 앱

## Superpowers 방법론

이 프로젝트는 [superpowers](https://github.com/obra/superpowers) 기반 개발 방법론을 사용한다.

**트리거 규칙:**
- 새 기능 추가 → `brainstorming` 스킬 먼저 사용
- 구현 계획 수립 → `writing-plans` 스킬 사용
- 계획 실행 → `executing-plans` 또는 `subagent-driven-development` 스킬 사용
- 버그/오류 발생 → `systematic-debugging` 스킬 사용
- 코드 작성 전 → `test-driven-development` 스킬 사용
- 기능 완성 후 → `verification-before-completion` 스킬 사용
- PR/머지 전 → `finishing-a-development-branch` 스킬 사용
- 독립적인 작업 여러 개 → `dispatching-parallel-agents` 스킬 사용

**원칙:** 작업 시작 전 반드시 관련 스킬이 있는지 확인하고, 1%라도 해당 가능성이 있으면 스킬을 먼저 실행한다.

## 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-08 | 초기 구성 | 전체 | superpowers 방법론 적용 |
